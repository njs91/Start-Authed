describe('homepage', () => {
    it('successfully loads homepage', () => {
        cy.visit('/');

        // @todo: get it to run the client (& server) automatically? otherwise have to run them manually
    });
});
